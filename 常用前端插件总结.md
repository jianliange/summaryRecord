# 富文本

## wangeditor
### 官网地址
https://www.kancloud.cn/wangfupeng/wangeditor3/332599
### 代码（VUE项目）
```js
    creatEditer() {
      this.editor = new E(this.$refs.editor)

      // 菜单选项配置
      this.editor.customConfig.menus = [
        'bold', // 粗体
        'list'
      ]
      // 富文本绑定的值
      this.editor.customConfig.onchange = (html) => {
        this.editorVal = html
      }
      this.editor.create()
      
      // 富文本禁用控制
      if (this.type === 'show') {
        this.editor.$textElem.attr('contenteditable', false)
      }
      this.editor.txt.html(this.casepointResult.sametradedescribe)

      // 通过监听input事件决解第一次鼠标全选删除不触发onchange事件决解
      this.$refs['editor'].addEventListener('input', this.inputEvent)
    },
    inputEvent() {
      let html = this.editor1.txt.html()
      if (html === '' || html === '<p><br></p>') {
        this.editor.customConfig.onchange()
      }
    },
```
### 问题
#### 层叠问题
 - 调整富文本dom下 .w-e-text-container和.w-e-menu 的z-index属性
#### 第一次鼠标全选删除不触发onchange事件
 - 通过监听input事件决解第一次鼠标全选删除不触发onchange事件决解

# 日期

## moment
### 代码
```js
import moment from 'moment'

// 改变日期
export function changeDate(value) {
  return moment(value).format('YYYY-MM-DD')
}
```

# 加密

## rsaEncrypt
### 代码
```js
import JSEncrypt from 'jsencrypt/bin/jsencrypt'

// 密钥对生成 http://web.chacuo.net/netrsakeypair

const publicKey = ''

const privateKey = ''

// 加密
export function encrypt(txt) {
  const encryptor = new JSEncrypt()
  encryptor.setPublicKey(publicKey) // 设置公钥
  return encryptor.encrypt(txt) // 对需要加密的数据进行加密
}

// 解密
export function decrypt(txt) {
  const encryptor = new JSEncrypt()
  encryptor.setPrivateKey(privateKey)
  return encryptor.decrypt(txt)
}
```
### 存在的问题
- 字符太长会出错
- Boolean值加密有问题

## AES
```js
import CryptoJS from 'crypto-js/crypto-js'

export function encryptAES (key,content) {
  var key = CryptoJS.enc.Utf8.parse(key) // abcdefghigkliopk密码，16位
  var encryptResult = CryptoJS.AES.encrypt(content, key, {
    iv: CryptoJS.enc.Utf8.parse('0102030405060708'), // 0102030405060708偏移量，16位
    mode: CryptoJS.mode.ECB, // aes加密模式cbc
    padding: CryptoJS.pad.Pkcs7 // 填充
  })
  var result = String(encryptResult)// 把object转化为string
  return result
}

// AES 解密
export function decryptAES (key,word) {
  const keyStr = key
  var key = CryptoJS.enc.Utf8.parse(keyStr)
  var decrypt = CryptoJS.AES.decrypt(word, key, { mode: CryptoJS.mode.ECB, padding: CryptoJS.pad.Pkcs7 })
  return CryptoJS.enc.Utf8.stringify(decrypt).toString()
}

```


# 文件类
## html2canvas
官网地址：http://html2canvas.hertzen.com/
## jsPDF
官网地址：http://mrrio.github.io/
## html生成PDF预览和下载案例
### 问题记录
- 每一页截断，可能将一行文字中间截断问题
  - 生成canvas，按照固定比例截断每一页，生成之前，从这一页底部开始循环，如果有连续10行都没有内容，则可以截断；
  - 因为有可能是表格，所以可能有边框，所以判断没有内容，是根据这一行中不是白色的像素点不能超过20个；
- 添加页眉，页码问题
  - 因为不知道在html页面哪里开始截断；所以只能在页面先生成一个页面样式，然后使用html2canvas截取成图片，在canvas转换生成每一页PDF的之前，jspdf先添加生成的页眉图片，然后再添加内容，然后再添加页码。
- 实现PDF预览和下载
- 不需要展现生成的html
  - 一进页面显示加载提示，将生成的html遮罩住，然后生成PDF后再关闭当前窗口
- 内容太多导致下载下来的PDF页面黑屏
  - 生成canvas有一个最大高度，超过这个高度可能生成不了canvas，所以有黑屏
  - 解决方法：html2canvas生成canvas，将获取整个html页面改成获取根据类名来获取多个canvas

### vue代码文件
```js
<template>
  <div class="app-container">
    <div class="mask">
      <div v-if="!isAddPage" class="tips">
        <i class="el-icon-loading"/>
        附件正在生成中......
      </div>
      <div v-else class="tips">
        <i class="el-icon-loading"/>
        附件正在生成第<span class="pdfProgress">{{ pdfPage }}</span>页，共<span class="pdfTotal">{{ pdfProgress }}</span>页......
      </div>
    </div>
    <!-- <div class="head-content">
      <div class="pdfTip">
        <div>正在生成第
          <span class="pdfProgress">{{ pdfPage }}</span>页，共
          <span class="pdfTotal">{{ pdfProgress }}</span>页...
        </div>
      </div>
    </div> -->

    <!-- 页码 -->
    <div class="hideDiv">
      <div id="title">
        <p> {{ pdfContent.supplierName }}</p>
      </div>
      <div id="footer">
        第<span class="pdfProgress">{{ pdfPage }}</span>页
      </div>
    </div>

    <div id="pdfDom">
      <div id="cover">
        <div class="coverContent">
          <h1 style="font-size: 22px;">{{ pdfContent.supplierName }}</h1>
          <h2 style="font-size: 22px;">《{{ pdfContent.insuredName }}调查报告》</h2>
        </div>
      </div>
      <div class="content">
        <div class="casepoint">
          <h1 style="text-align: center;font-size: 22px;">调查报告</h1>
          <p class="title3"><strong>一、基本信息</strong></p>
          <table class="xwtable">
            <tbody>
              <tr>
                <td>调查号</td>
                <td>{{ pdfContent.taskno }}</td>
                <td>被保人姓名</td>
                <td>{{ pdfContent.insuredName }}</td>
              </tr>
              <tr>
                <td>首保日期</td>
                <td>{{ pdfContent.firstInsuranceDate }}</td>
                <td>被保人身份证</td>
                <td>{{ pdfContent.idcardno }}</td>
              </tr>
              <tr>
                <td>委托日期</td>
                <td>{{ pdfContent.dispatchDate }}</td>
                <td>报告日期</td>
                <td>{{ pdfContent.reportDate }}</td>
              </tr>
              <tr>
                <td>调查员</td>
                <td>{{ pdfContent.investigator }}</td>
                <td>复核员</td>
                <td>{{ pdfContent.reviewer }}</td>
              </tr>
              <tr>
                <td>保单信息</td>
                <td colspan="3" >{{ pdfContent.policyInfo }}</td>
              </tr>
              <tr>
                <td>索赔事由</td>
                <td colspan="3" >{{ pdfContent.doubtfuldescription }}</td>
              </tr>
              <tr>
                <td>调查要求</td>
                <td colspan="3" >{{ pdfContent.initiatedemand }}</td>
              </tr>
              <!-- <tr>
                <td>案件备注</td>
                <td colspan="3">{{ pdfContent.taskno }}</td>
              </tr> -->
            </tbody>
          </table>

          <p class="title3"><strong>二、调查情况</strong></p>
        </div>
        <!-- 面访 -->
        <div v-if="pdfContent.surveyPointInterviewList.length>0" >
          <div v-for="(point,idx) in pdfContent.surveyPointInterviewList" :key="'1'+idx" class="casepoint">
            <p class="title3"><strong>{{ idx+1 }}、{{ point.hospitalname }}</strong></p>
            <p v-if="point.provincecity">地址：{{ point.provincecity[0]+point.provincecity[1] +point.provincecity[2] }}</p>
            <table class="xwtable">
              <tbody>
                <tr>
                  <td>面访对象</td>
                  <td>{{ point.faceinterview }}</td>
                  <td>与被保险人关系</td>
                  <td>{{ getRelationship(point.relationshipinsured) }}</td>
                </tr>
                <tr>
                  <td>出险就诊经过</td>
                  <td colspan="3"><span v-html="point.processofrisk"/></td>
                </tr>
                <tr>
                  <td>既往就诊、体检情况</td>
                  <td colspan="3"><span v-html="point.passmedicalexamination"/></td>
                </tr>
                <tr>
                  <td>生活轨迹</td>
                  <td colspan="3"><span v-html="point.lifetrack"/></td>
                </tr>
                <tr>
                  <td>工作情况</td>
                  <td colspan="3"><span v-html="point.workingconditions"/></td>
                </tr>
                <tr>
                  <td>社保、同业情况</td>
                  <td colspan="3"><span v-html="point.socialindustrysituation"/></td>
                </tr>
                <tr>
                  <td>获取证据材料</td>
                  <td colspan="3"><span v-html="point.evidence"/></td>
                </tr>
                <tr>
                  <td>备注</td>
                  <td colspan="3"><span v-html="point.detailremark"/></td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
        <!-- 社保 -->
        <div v-if="pdfContent.surveyPointSocialList.length>0">
          <div v-for="(point,idx) in pdfContent.surveyPointSocialList" :key="'2'+idx" class="casepoint">
            <p class="title3"><strong>{{ pdfContent.surveyPointInterviewList.length + idx + 1 }}、{{ point.hospitalname }}</strong></p>
            <p v-if="point.provincecity">地址：{{ point.provincecity[0]+point.provincecity[1] +point.provincecity[2] }}</p>
            <table class="xwtable">
              <tbody>
                <tr>
                  <td>排查条件</td>
                  <td colspan="3">{{ point.outpatienttimeandcondition }}</td>
                </tr>
                <tr>
                  <td>排查结果</td>
                  <td colspan="3">
                    <!-- <span v-if="point.investigationresults==='01'">排查有记录，记录示：被保险人于{{ changeDate(point.insureddate) }} 加入{{ point.insuredname }} ，<span v-if="point.reimbursementflag==='02'">报销无记录。</span><span v-if="point.reimbursementflag==='01'">报销记录示 ：<br><span v-for="item in point.surveyPointInfoList" :key="item.id" >{{ changeDate(item.reimbursementdate) }}在{{ item.reimbursementagency }}{{ getReimbursementreasons(item.reimbursementreasons) }}报销{{ item.reimbursemenmoney }}元；<br></span></span></span>
                    <span v-if="point.investigationresults==='02'||point.investigationresults==='03'">排查{{ point.investigationresults==='02'?'无记录：':'无法排查：' }}<br>{{ point.investigationdescription }}</span> -->
                    <span v-if="point.investigationresults==='01'">排查有记录，记录示：被保险人于{{ changeDate(point.insureddate) }} 加入{{ point.insuredname }} ，<span v-if="point.reimbursementflag==='02'">报销无记录。</span><span v-if="point.reimbursementflag==='01'">报销记录示 ：<br><span v-for="item in point.surveyPointInfoList" :key="item.id" ><span v-if="item.blackflag==='01'"><strong>{{ changeDate(item.reimbursementdate) }}因{{ item.diagnosis }}在{{ item.reimbursementagency }}{{ getReimbursementreasons(item.reimbursementreasons) }}报销<span v-if="item.reimbursemenmoney!==''&&item.reimbursemenmoney!==undefined&&item.reimbursemenmoney!==null">{{ item.reimbursemenmoney }}元</span><span v-if="item.reimbursemenmoney===''||item.reimbursemenmoney===undefined||item.reimbursemenmoney===null">，金额未知</span>；</strong><br></span><span v-if="item.blackflag!=='01'">{{ changeDate(item.reimbursementdate) }}因{{ item.diagnosis }}在{{ item.reimbursementagency }}{{ getReimbursementreasons(item.reimbursementreasons) }}报销<span v-if="item.reimbursemenmoney!==''&&item.reimbursemenmoney!==undefined&&item.reimbursemenmoney!==null">{{ item.reimbursemenmoney }}元</span><span v-if="item.reimbursemenmoney===''||item.reimbursemenmoney===undefined||item.reimbursemenmoney===null">，金额未知</span>；<br></span></span></span></span>
                    <span v-if="point.investigationresults==='02'||point.investigationresults==='03'">排查{{ point.investigationresults==='02'?'无记录：':'无法排查：' }}<br>{{ point.investigationdescription }}</span>
                  </td>
                </tr>
                <tr>
                  <td>获取证据材料</td>
                  <td colspan="3">{{ point.evidence }}</td>
                </tr>
                <tr>
                  <td>备注</td>
                  <td colspan="3">{{ point.detailremark }}</td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
        <!-- 医院 -->
        <div v-if="pdfContent.surveyPointHosptialList.length>0">
          <div v-for="(point,idx) in pdfContent.surveyPointHosptialList" :key="'3'+idx" class="casepoint">
            <p class="title3"><strong>{{ pdfContent.surveyPointInterviewList.length+pdfContent.surveyPointSocialList.length + idx + 1 }}、{{ point.hospitalname }}</strong></p>
            <p v-if="point.provincecity">地址：{{ point.provincecity[0]+point.provincecity[1] +point.provincecity[2] }}</p>
            <table class="xwtable">
              <tbody>
                <tr>
                  <td>住院排查条件</td>
                  <td colspan="3">{{ point.inpatientortimeandcondition }}</td>
                </tr>
                <tr>
                  <td>住院排查结果</td>
                  <td colspan="3">
                    <div v-if="point.hospitalinvestigation==='01'">排查发现被保险人住院记录{{ point.surveyPointInfoList01.length }}条：<br>
                      <span v-for="(item,idx) in point.surveyPointInfoList01" :key="item.id" ><span v-if="item.blackflag==='01'"><strong>第{{ numberToChinese(idx+1) }}次住院时间：{{ changeDate(item.admissiondate[0]) }}至{{ changeDate(item.admissiondate[1]) }} <br>
                        <span v-show="item.department">科室：{{ item.department }} <br></span>
                        <span v-show="item.chiefcomplaint">主诉：{{ item.chiefcomplaint }}<br></span>
                        <span v-show="item.presentillnesshistory">现病史：{{ item.presentillnesshistory }}<br></span>
                        <span v-show="item.diagnosis">出院诊断：{{ item.diagnosis }} <br></span>
                        <span v-show="item.pastsymptoms">既往史：{{ item.pastsymptoms }} <br></span>
                        <span v-show="item.checkauxiliaryitems">辅助检查：{{ item.checkauxiliaryitems }}<br></span>
                        <span v-show="item.operationname">手术名称：{{ item.operationname }} <br></span>
                      <span v-show="item.pathologicalreport">病理报告：{{ item.pathologicalreport }}<br></span><strong/></strong></span>
                        <span v-else>第{{ numberToChinese(idx+1) }}次住院时间：{{ changeDate(item.admissiondate[0]) }}至{{ changeDate(item.admissiondate[1]) }} <br>
                          <span v-show="item.department">科室：{{ item.department }} <br></span>
                          <span v-show="item.chiefcomplaint">主诉：{{ item.chiefcomplaint }}<br></span>
                          <span v-show="item.presentillnesshistory">现病史：{{ item.presentillnesshistory }}<br></span>
                          <span v-show="item.diagnosis">出院诊断：{{ item.diagnosis }} <br></span>
                          <span v-show="item.pastsymptoms">既往史：{{ item.pastsymptoms }} <br></span>
                          <span v-show="item.checkauxiliaryitems">辅助检查：{{ item.checkauxiliaryitems }}<br></span>
                          <span v-show="item.operationname">手术名称：{{ item.operationname }} <br></span>
                        <span v-show="item.pathologicalreport">病理报告：{{ item.pathologicalreport }}<br></span></span>
                      </span>
                    </div>
                    <div v-if="point.hospitalinvestigation==='02'||point.hospitalinvestigation==='03'">排查{{ point.hospitalinvestigation==='02'?'无记录：':'无法排查：' }}<br>{{ point.hospitaldescription }}
                    </div>
                  </td>
                </tr>
                <tr>
                  <td>门诊排查条件</td>
                  <td colspan="3">{{ point.outpatienttimeandcondition }}</td>
                </tr>
                <tr>
                  <td>门诊排查结果</td>
                  <td colspan="3">
                    <div v-if="point.outpatientinvestigation==='01'&&point.surveyPointInfoList02.length>0">
                      <span>排查有记录，发现被保险人就诊记录{{ point.surveyPointInfoList02.length }}条，记录示：</span><br><span v-for="item in point.surveyPointInfoList02" :key="item.id" ><span v-if="item.blackflag==='01'"><strong>{{ changeDate(item.visittime[0]) }}至{{ changeDate(item.visittime[1]) }}诊断：{{ item.diagnosis }} <br><strong/></strong></span><span v-else>{{ changeDate(item.visittime[0]) }}至{{ changeDate(item.visittime[1]) }}诊断：{{ item.diagnosis }} <br></span></span>
                    </div>
                    <div v-if="point.outpatientinvestigation==='02'||point.outpatientinvestigation==='03'">排查{{ point.outpatientinvestigation==='02'?'无记录：':'无法排查：' }}<br>{{ point.outpatientdescription }}
                    </div>
                  </td>
                </tr>
                <tr>
                  <td>获取证据材料</td>
                  <td colspan="3">{{ point.evidence }}</td>
                </tr>
                <tr>
                  <td>备注</td>
                  <td colspan="3">{{ point.detailremark }}</td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
        <!-- 体检 -->
        <div v-if="pdfContent.surveyPointPhysicalList.length>0" class="casepoint">
          <div v-for="(point,idx) in pdfContent.surveyPointPhysicalList" :key="'4'+idx" >
            <p class="title3"><strong>{{ pdfContent.surveyPointInterviewList.length+pdfContent.surveyPointSocialList.length+pdfContent.surveyPointHosptialList.length + idx + 1 }}、{{ point.hospitalname }}</strong></p>
            <p v-if="point.provincecity">地址：{{ point.provincecity[0]+point.provincecity[1] +point.provincecity[2] }}</p>
            <table class="xwtable">
              <tbody>
                <tr>
                  <td>体检情况</td>
                  <td colspan="3">{{ point.physicalexaminationflag==='01'?'有':'无' }}</td>
                </tr>
                <tr>
                  <td>排查结果</td>
                  <td colspan="3"><span v-html="point.detailremark"/></td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>

        <!-- 同业 -->
        <div v-if="pdfContent.surveyPointSameList.length>0" class="casepoint">
          <div v-for="(point,idx) in pdfContent.surveyPointSameList" :key="'5'+idx" >
            <p class="title3"><strong>{{ pdfContent.surveyPointInterviewList.length+pdfContent.surveyPointSocialList.length+pdfContent.surveyPointHosptialList.length+pdfContent.surveyPointPhysicalList.length + idx + 1 }}、{{ point.hospitalname }}</strong></p>
            <p v-if="point.provincecity">地址：{{ point.provincecity[0]+point.provincecity[1] +point.provincecity[2] }}</p>
            <table class="xwtable">
              <tbody>
                <tr>
                  <td>同业投保情况</td>
                  <td colspan="3">{{ point.sametradeflag==='01'?'有':'无' }}</td>
                </tr>
                <tr>
                  <td>排查结果</td>
                  <td colspan="3"><span v-html="point.sametradedescribe"/></td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
        <!-- 其他 -->
        <div v-if="pdfContent.surveyPointOtherList.length>0" class="casepoint">
          <div v-for="(point,idx) in pdfContent.surveyPointOtherList" :key="'6'+idx" >
            <p class="title3"><strong>{{ pdfContent.surveyPointInterviewList.length+pdfContent.surveyPointSocialList.length+pdfContent.surveyPointHosptialList.length+pdfContent.surveyPointPhysicalList.length+pdfContent.surveyPointSameList.length + idx + 1 }}、{{ point.hospitalname }}</strong></p>
            <p v-if="point.provincecity">地址：{{ point.provincecity[0]+point.provincecity[1] +point.provincecity[2] }}</p>
            <table class="xwtable">
              <tbody>
                <tr>
                  <td>排查结果</td>
                  <td colspan="3"><span v-html="point.detailremark"/></td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>

        <div v-if="pdfContent.analysissuggestions" class="casepoint">
          <p class="title3"><strong>三、分析/建议</strong></p>
          <table class="xwtable">
            <tbody>
              <tr>
                <td colspan="4" v-html="pdfContent.analysissuggestions"/>
              </tr>
            </tbody>
          </table>
        </div>

        <div v-if="pdfContent.conclusionDescription" class="casepoint">
          <p v-if="pdfContent.analysissuggestions" class="title3"><strong>四、调查结论</strong></p>
          <p v-else class="title3"><strong>三、调查结论</strong></p>
          <table class="xwtable">
            <tbody>
              <tr>
                <td colspan="4" >{{ pdfContent.conclusionDescription }}</td>
              </tr>
            </tbody>
          </table>
        </div>

      </div>
    </div>

  </div>
</template>
<script>
import html2Canvas from 'html2canvas'
import jsPDF from 'jspdf'
import { Loading } from 'element-ui'
import { uploadAndPreviewPDF } from '@/api/investigate/investigateHandle'
import { decrypt } from '@/utils/rsaEncrypt'
import { changeDate, numberToChinese } from '@/utils/commenMethods'

export default{
  name: 'SeePdf',
  dicts: ['relationship', 'reimbursementreasons', 'department'],
  data() {
    return {
      pdfProgress: 1,
      pdfPage: 1,
      pdfTotal: null,
      type: '',
      taskno: '',
      pdfContent: '',
      isDownloaded: false, // 是否生成PDF
      isOver: false, // 是否生成完毕
      a4Width: 595.28,
      a4Height: 841.89,
      pdf: null,
      canvas: [],
      a4HeightRef: 0,
      position: 0,
      leftHeight: 0,
      pageFooter: null,
      canvasFooter: null,
      pageTitle: null,
      canvasTitle: null,
      canvasTitleH: null,
      canvasbreakHeight: 0,
      canvasIndex: 0,
      pageData: null,
      isAddPage: false
    }
  },
  watch: {
    isOver: function(newVal) {
      if (newVal) {
        setTimeout(function() {
          window.close()
        }, 300)
      }
    }
  },
  created() {
    if (this.$route.query) {
      this.querys = JSON.parse(decodeURI(this.$route.query.data))
      this.taskno = decrypt(this.querys.taskno) || ''
      this.type = this.querys.type
    }
    this.uploadAndPreviewPDF()

    // 初始化PDF
    this.pdf = new jsPDF('x', 'pt', 'a4')
    this.pdf.page = 1
    this.pdf.setDisplayMode('fullwidth', 'continuous', 'FullScreen')
  },
  updated() {
    if (!this.isDownloaded) {
      this.getPdfFun()
    }
  },
  methods: {
    uploadAndPreviewPDF() {
      uploadAndPreviewPDF(this.taskno).then(res => {
        if (res.status === '1') {
          this.pdfContent = res.data
          // 医院案件点，住院诊断和门诊分开
          for (let i = 0; i < this.pdfContent.surveyPointHosptialList.length; i++) {
            if (this.pdfContent.surveyPointHosptialList[i].surveyPointInfoList.length > 0) {
              this.pdfContent.surveyPointHosptialList[i].surveyPointInfoList01 = this.pdfContent.surveyPointHosptialList[i].surveyPointInfoList.filter((item) => {
                return item.infotype === '0101'
              })
              this.pdfContent.surveyPointHosptialList[i].surveyPointInfoList02 = this.pdfContent.surveyPointHosptialList[i].surveyPointInfoList.filter((item) => {
                return item.infotype === '0102'
              })
            } else {
              this.pdfContent.surveyPointHosptialList[i].surveyPointInfoList01 = []
              this.pdfContent.surveyPointHosptialList[i].surveyPointInfoList02 = []
            }
          }
          // this.getPdfFun()
        } else {
          this.$message.error(res.data.errMsg)
        }
      }).catch(err => {
        console.log(err)
        this.$message.error('出错了，请稍后再试！')
      })
    },
    // 获取与被保人关系
    getRelationship(val) {
      return this.dict.label.relationship[val]
    },
    // 获取报销原因
    getReimbursementreasons(val) {
      return this.dict.label.reimbursementreasons[val]
    },
    // 获取科室
    getDepartment(val) {
      return this.dict.label.department[val]
    },
    // 日期格式化 yyyy-mm-dd
    changeDate(val) {
      return changeDate(val)
    },
    numberToChinese(num) {
      return numberToChinese(num)
    },
    getPdfFun() {
      this.isDownloaded = true
      if (this.type === '01') {
        this.getPdf('PICCH-调查报告-' + this.pdfContent.insuredName + '-' + this.pdfContent.taskno, true)
      } else if (this.type === '02') {
        this.getPdf('PICCH-调查报告-' + this.pdfContent.insuredName + '-' + this.pdfContent.taskno, false)
      }
    },
    async getPdf(title, show) {
      // 生成页眉图片
      this.canvasTitle = await html2Canvas(document.getElementById('title'))
      this.pageTitle = this.canvasTitle.toDataURL('image/jpeg', 1.0)

      // 生成首页图片
      const canvasCover = await html2Canvas(document.querySelector('#cover'), {
        allowTaint: true,
        scale: 2
      })

      // 生成首页PDF
      this.pdf.addImage(canvasCover.toDataURL('image/jpeg', 1.0), 'JPEG', 0, 0, this.a4Width, this.a4Width / canvasCover.width * canvasCover.height)

      // 生成内容 6个案件点的图片
      const doms = document.getElementsByClassName('casepoint')
      for (let i = 0; i < doms.length; i++) {
        if (doms[i]) {
          const canvas = await html2Canvas(doms[i], {
            allowTaint: true,
            scale: 2
          })
          this.canvas.push(canvas)
        }
      }

      this.canvas1 = document.createElement('canvas')
      // 一页pdf显示html页面生成的canvas高度; -300为了每页PDF头部和底部留有空位
      this.a4HeightRef = Math.floor(this.canvas[0].width / this.a4Width * this.a4Height) - 200
      this.position = 0
      this.addPage()
      for (let canvasIndex = 0; canvasIndex < this.canvas.length; canvasIndex++) {
        this.position = 0
        // 未生成pdf的html页面高度
        this.leftHeight = this.canvas[canvasIndex].height
        console.log('canvasIndex  html页面高度', canvasIndex, this.leftHeight)
        // this.pageData = await this.canvas[i].toDataURL('image/jpeg', 1.0)
        this.createImpl(this.canvas[canvasIndex])
      }
      const that = this
      // 根据生成的页数，给每一页PDF加上页码
      this.isAddPage = true
      for (let page = 0; page < this.pdfProgress; page++) {
        this.pdfPage = page + 1
        this.canvasFooter = await html2Canvas(document.getElementById('footer'))
        this.pageFooter = this.canvasFooter.toDataURL('image/jpeg', 1.0)
        that.pdf.setPage(page)

        // 添加页眉
        this.pdf.addImage(this.pageTitle, 'JPEG', 0, 0, this.a4Width, (this.a4Width / this.canvasTitle.width * this.canvasTitle.height).toFixed(2) / 1)
        // 添加页码
        that.pdf.addImage(this.pageFooter, 'JPEG', 0, this.a4Height - 30, this.a4Width, (this.a4Width / this.canvasFooter.width * this.canvasFooter.height).toFixed(2) / 1)
      }
      // 所有内容都生成完毕
      if (!show) {
        that.pdf.save(title + '.pdf')
        that.isOver = true
      } else {
        // 在新窗口查看 pdf文件 【新技能】
        that.pdf.output('dataurlnewwindow')
        that.isOver = true
      }
    },
    async createImpl(canvas) {
      console.log('这页PDF剩余的高度', this.a4LeftHeight)
      console.log('canvas剩余的高度', this.leftHeight)
      console.log('canvas开始截取的位置高度', this.position)
      if (this.leftHeight > 0) {
        if (this.leftHeight > this.a4LeftHeight) {
          var checkCount = 0
          var i = this.position + this.a4LeftHeight
          console.log('i  position', i, this.position)
          // 从定好的高度页面，底部开始循环遍历canvas的每个点，找到可以截断的地方
          for (i; i >= this.position; i--) {
            var count = 0
            var isWrite = true
            for (var j = 0; j < canvas.width; j++) {
              var c = canvas.getContext('2d').getImageData(j, i, 1, 1).data
              // 如果该单位的颜色不是白色c[0]  c[1]  c[2] 分别代表r g b 255
              if (c[0] != 0xff || c[1] != 0xff || c[2] != 0xff) {
                count++
              }
              // 如果这行有连续20个单位都不是白色，退出当前循环
              if (count > 20) {
                isWrite = false
                break
              }
            }
            // 如果有连续10行都是白色的，canvas在这里可以截断了
            if (isWrite) {
              checkCount++
              if (checkCount >= 20) {
                break
              }
            } else {
              checkCount = 0
            }
          }
          console.log('i', i)
          this.height = i - this.position
          if (this.height <= 0) { // 没找到截断点,新增一页PDF
            // PDF剩余可用高度
            this.a4LeftHeight = (this.a4HeightRef - 150).toFixed(2) / 1
            // 先添加一张PDF
            this.pdf.addPage()
            this.pdfProgress = this.pdfProgress + 1
            console.log('新增一页PDF  this.pdfProgress', this.pdfProgress)
            // 添加页眉
            this.pdf.addImage(this.pageTitle, 'JPEG', 0, 0, this.a4Width, (this.a4Width / this.canvasTitle.width * this.canvasTitle.height).toFixed(2) / 1)

            this.height = this.leftHeight
            this.createImpl(canvas)
            return
          }
        } else {
          this.height = this.leftHeight
        }
        this.canvas1.width = canvas.width
        this.canvas1.height = this.height

        var ctx = this.canvas1.getContext('2d')
        ctx.drawImage(canvas, 0, this.position, canvas.width, this.height, 0, 0, canvas.width, this.height)
        console.log('canvas截断的位置', this.position, this.height)
        console.log('PDF生成的位置', this.a4HeightRef - this.a4LeftHeight, this.height)
        this.pdf.addImage(this.canvas1.toDataURL('image/jpeg', 1.0), 'JPEG', 0, this.a4Width / this.canvas1.width * (this.a4HeightRef - this.a4LeftHeight), this.a4Width, this.a4Width / this.canvas1.width * this.height)

        this.leftHeight -= this.height
        this.position += this.height
        this.a4LeftHeight -= this.height + 50

        if (this.leftHeight > 0) { // 第i个canvas还未截取完毕
          await this.createImpl(canvas)
        }
      }
    },
    async addPage() {
      // PDF页面剩余的高度
      this.a4LeftHeight = (this.a4HeightRef - 150).toFixed(2) / 1
      // 先添加一张PDF
      this.pdf.addPage()
      this.pdfProgress = this.pdfProgress + 1
      console.log('新增一页PDF  this.pdfProgress', this.pdfProgress)
    }
  }
}
</script>
<style scoped>
.mask{
  position: fixed;
  left:0;
  top:0;
  width:100%;
  height:100%;
  color:#409eff;
  z-index: 9999;
  background-color:#ffffff;
}
.casepoint{
  padding: 0 50px;
}
.mask .tips{
  position: absolute;
  top: 45%;
  width:100%;
  font-size:18px;
  text-align: center;
}
.title3{
  font-size: 16px;
}
#title {
  width:800px;
  text-align: center;
  padding:10px 60px 0;
  color:#808080;
}
#title p{
  font-size: 14px;
  padding-bottom: 5px;
  border-bottom: 1px solid #c4c7ce;
}
#footer {
  font-size: 12px;
  color:#c4c7ce;
  width:800px;
  text-align: center;
}
#pdfDom {
  width:800px;
  letter-spacing: 2px;
}
#cover{
  display:flex;
  height: 1020px;
}
#cover .coverContent{
   margin: auto;
   text-align:center;
}
p{
  font-size: 14px;
}
td .label {
  width:150px;
  display:inline-block;
}
td, td span{
  white-space:pre-line;
}
/************ Table ************/
.xwtable {width: 100%;border-collapse: collapse;border: 1px solid ;}
.xwtable thead td {font-size: 14px;text-align: center;border: 1px solid ; font-weight:bold;}
.xwtable tbody tr {background: #fff;font-size: 14px;}
.xwtable td{line-height:28px;text-align: left;padding:4px 10px 3px 10px;width:150px;height: 18px;border: 1px solid ;word-break: break-word;}
</style>

```
## 复制粘贴插件
```js
import Vue from 'vue'
import Clipboard from 'clipboard'

function clipboardSuccess() {
  Vue.prototype.$message({
    message: '复制成功',
    type: 'success',
    duration: 1500
  })
}

function clipboardError() {
  Vue.prototype.$message({
    message: '复制失败',
    type: 'error'
  })
}

export default function handleClipboard(text, event) {
  const clipboard = new Clipboard(event.target, {
    text: () => text
  })
  clipboard.on('success', () => {
    clipboardSuccess()
    clipboard.off('error')
    clipboard.off('success')
    clipboard.destroy()
  })
  clipboard.on('error', () => {
    clipboardError()
    clipboard.off('error')
    clipboard.off('success')
    clipboard.destroy()
  })
  clipboard.onClick(event)
}

```





## jszip
官网：https://stuk.github.io/jszip/documentation/examples.html

##  compressorjs 
- compressorjs 是一个开源的图片处理库，提供了图片压缩、图片旋转等能力。
链接：https://github.com/fengyuanchen/compressorjs

## 大文件切片上传
官网地址：https://github.com/simple-uploader/vue-uploader/blob/master/README_zh-CN.md
参考链接：
 - https://www.cnblogs.com/xiahj/p/vue-simple-uploader.html
 - https://gitee.com/luckytuan/fast-loader
 - https://github.com/simple-uploader/Uploader/blob/develop/README_zh-CN.md
 - https://github.com/simple-uploader/vue-uploader/blob/master/README_zh-CN.md

```html
<template>
  <div>
    <div class="container">

      <!--操作栏 begin-->
      <div class="handle-box" style="margin-bottom: 10px;">
        <button type="button" class="el-button el-icon-upload2 el-button--default el-button--small" @click="uploadFiles()">上传文件</button>
        <span slot="tip" class="el-upload__tip">支持扩展名：.avi .mp4 .mov .3gp .mp3 .wav .ogg .aac .m4a</span>
      </div>
      <!--操作栏 end-->

      <!-- table主要区域 begin -->
      <el-table
        :data="tableData"
        :header-cell-style="{background:'#fafafa',textAlign:'center'}"
        size="mini"
        style="width: 100%;"
      >
        <el-table-column prop="name" align="center" label="附件标题" show-overflow-tooltip/>
        <el-table-column label="操作" align="center">
          <template slot-scope="scope">
            <el-button
              type="text"
              icon="el-icon-download"
              class="blue"
              @click="handleDownload(scope.$index, scope.row)"
            />
            <el-button
              v-if="scope.row.bussinessNo==undefined"
              :disabled="scope.row.creator!==username&&investigationType!=='01'"
              type="text"
              icon="el-icon-delete"
              class="red"
              @click="handleDelete(scope.$index, scope.row)"
            />
          </template>
        </el-table-column>
      </el-table>
    </div>

    <!--上传附件弹出框 -->
    <el-dialog
      :visible.sync="uploadVisible"
      :append-to-body="true"
      :close-on-click-modal="false"
      :before-close="dialogClosed"
      title="其他附件上传"
      center
      width="60%">
      <!-- 上传器 -->
      <uploader
        ref="uploader"
        :options="options"
        :auto-start="false"
        :file-status-text="fileStatusText"
        class="uploader-ui"
        @file-added="onFileAdded"
        @file-success="onFileSuccess"
        @file-error="onFileError"
        @file-removed="onRemove">
        <uploader-unsupport/>
        <uploader-drop>
          <div>
            <uploader-btn id="global-uploader-btn" ref="uploadBtn" :attrs="attrs">选择文件<i class="el-icon-upload el-icon--right"/></uploader-btn>
            <span slot="tip" class="el-upload__tip">支持扩展名：.avi .mp4 .mov .3gp .mp3 .wav .ogg .aac .m4a</span>
          </div>
        </uploader-drop>
        <uploader-list/>
      </uploader>
    </el-dialog>
  </div>
</template>

<script>
import { deleteAttachment, mergeFile, deleteAttachmentFileNew } from '@/api/investigate/investigateHandle'
import { encrypt } from '@/utils/rsaEncrypt'
import SparkMD5 from 'spark-md5'

export default {
  name: 'Basetable',
  props: {
    OtherAnnex: {
      type: Array,
      default: function() {
        return []
      }
    },
    investigateNo: {
      type: String,
      default: function() {
        return ''
      }
    },
    investigationType: {
      type: String,
      default: function() {
        return ''
      }
    }
  },
  data() {
    return {
      uploadVisible: false,
      options: {
        // 目标上传 URL，默认POST
        target: process.env.FILE_APT + '/invest/uploader/chunk',
        // 分块大小(单位：字节) 1024*1024*5
        chunkSize: '5242880',
        // 上传文件时文件内容的参数名，对应chunk里的Multipart对象名，默认对象名为file
        fileParameterName: 'upfile',
        // 失败后最多自动重试上传次数
        maxChunkRetries: 3,
        // 是否开启服务器分片校验，对应GET类型同名的target URL
        testChunks: true,
        query: { taskNo: this.investigateNo, username: this.$store.state.user.user.username },
        /*
                服务器分片校验函数，判断秒传及断点续传,传入的参数是Uploader.Chunk实例以及请求响应信息
                reponse码是successStatuses码时，才会进入该方法
                reponse码如果返回的是permanentErrors 中的状态码，不会进入该方法，直接进入onFileError函数 ，并显示上传失败
                reponse码是其他状态码，不会进入该方法，正常走标准上传
                checkChunkUploadedByResponse函数直接return true的话，不再调用上传接口
                */
        checkChunkUploadedByResponse: function(chunk, response_msg) {
          const objMessage = JSON.parse(response_msg)
          if (objMessage.skipUpload) {
            return true
          }
          return (objMessage.uploadedChunks || []).indexOf(chunk.offset + 1) >= 0
        }
      },
      attrs: {
        accept: ['.avi', '.mp4', '.mov', '.3gp', '.mp3', '.wav', '.ogg', '.aac', '.m4a']
      },
      fileStatusText: {
        success: '上传成功',
        error: '上传失败',
        uploading: '上传中',
        paused: '暂停',
        waiting: '等待上传'
      },
      isBig: true, // 文件是否超过500M
      isType: true, // 文件类型是否正确
      isRepeat: true, // 文件是否重名
      isdel: true, // 是否可以删除，因为点击暂存的时候，清空列表，会删除已上传的文件
      currentArr: [] // 本次上传的文件
    }
  },
  computed: {
    tableData: {
      get() {
        return this.OtherAnnex
      },
      set(value) {
        this.$emit('changeOtherAnnex', value)
      }
    },
    username() {
      return this.$store.state.user.user.username
    }
  },
  methods: {
    onFileAdded(file) {
      // 单个文件的大小限制500M
      const fileSizeLimit = 500 * 1024 * 1024
      this.isBig = true
      console.log('文件大小：' + file.size)
      console.log('限制大小：' + fileSizeLimit)
      if (file.size > fileSizeLimit) {
        this.isBig = false
        this.$message({
          showClose: true,
          message: '文件大小不能超过500M'
        })
        file.cancel()
        file.ignored = true
        return false
      }

      // 判断文件后缀名
      const fileName = file.name
      const pos = fileName.lastIndexOf('.')
      const lastName = fileName.substring(pos, fileName.length)
      this.isType = true
      if (lastName.toLowerCase() !== '.avi' && lastName.toLowerCase() !== '.mp4' &&
                lastName.toLowerCase() !== '.mov' && lastName.toLowerCase() !== '.3gp' &&
                lastName.toLowerCase() !== '.mp3' && lastName.toLowerCase() !== '.wav' &&
                lastName.toLowerCase() !== '.ogg' && lastName.toLowerCase() !== '.aac' &&
                lastName.toLowerCase() !== '.m4a') {
        this.isType = false
        this.$message({
          showClose: true,
          message: '文件类型不正确！'
        })
        file.cancel()
        file.ignored = true
        return false
      }

      // 判断是否重复文件名
      this.isRepeat = true
      this.tableData.forEach(item => {
        if (file.name.toUpperCase() === item.name.toUpperCase()) {
          this.isRepeat = false
        }
      })
      if (this.isRepeat && this.currentArr.length > 0) {
        this.currentArr.forEach(item => {
          if (file.name.toUpperCase() === item.name.toUpperCase()) {
            this.isRepeat = false
          }
        })
      }
      if (!this.isRepeat) {
        this.$message({
          showClose: true,
          message: '相同文件已存在！'
        })
        file.cancel()
        file.ignored = true
        return false
      }
      if (this.isType && this.isBig && this.isRepeat) {
        this.currentArr.push(file)
        this.computeMD5(file)
      }
    },
    /*
        第一个参数 rootFile 就是成功上传的文件所属的根 Uploader.File 对象，它应该包含或者等于成功上传文件；
        第二个参数 file 就是当前成功的 Uploader.File 对象本身；
        第三个参数就是 message 就是服务端响应内容，永远都是字符串；
        第四个参数 chunk 就是 Uploader.Chunk 实例，它就是该文件的最后一个块实例，如果你想得到请求响应码的话，chunk.xhr.status就是
        */
    onFileSuccess(rootFile, file, response, chunk) {
      file.taskNo = this.investigateNo
      mergeFile(file).then(responseData => {
        if (responseData.status === '1' && responseData.data != '0') {
          // 合并成功，触发父级合并数据
          const file = responseData.data
          file.name = file.fileName
          file.url = file.bussinessNo
          this.tableData.push(file)
        } else {
          file.cancel()
          file.ignored = true
          return false
          this.$message('文件上传失败！')
        }
      }).catch(function(error) {
        console.log('合并后捕获的未知异常：' + error)
      })
    },
    onFileError(rootFile, file, response, chunk) {
      console.log('上传完成后异常信息：' + response)
      const res = JSON.parse(response)
      this.$message(res.message)
    },

    /**
         * 计算md5，实现断点续传及秒传
         * @param file
         */
    computeMD5(file) {
      file.pause()

      const fileReader = new FileReader()
      const time = new Date().getTime()
      const blobSlice = File.prototype.slice || File.prototype.mozSlice || File.prototype.webkitSlice
      let currentChunk = 0
      const chunkSize = 10 * 1024 * 1024
      const chunks = Math.ceil(file.size / chunkSize)
      const spark = new SparkMD5.ArrayBuffer()
      // 由于计算整个文件的Md5太慢，因此采用只计算第1块文件的md5的方式
      const chunkNumberMD5 = 1

      loadNext()

      fileReader.onload = e => {
        spark.append(e.target.result)

        if (currentChunk < chunkNumberMD5) {
          loadNext()
        } else {
          const md5 = spark.end()
          file.uniqueIdentifier = md5
          // // 添加额外的参数
          // this.uploader.opts.query = {
          //     taskNo:this.investigateNo,
          //     id:''
          // }
          file.resume()
          console.log(`MD5计算完毕：${file.name} \nMD5：${md5} \n分片：${chunks} 大小:${file.size} 用时：${new Date().getTime() - time} ms`)
        }
      }

      fileReader.onerror = function() {
        this.error(`文件${file.name}读取出错，请检查该文件`)
        file.cancel()
      }

      function loadNext() {
        const start = currentChunk * chunkSize
        const end = ((start + chunkSize) >= file.size) ? file.size : start + chunkSize
        fileReader.readAsArrayBuffer(blobSlice.call(file.file, start, end))
        currentChunk++
        console.log('计算第' + currentChunk + '块')
      }
    },
    close() {
      this.uploader.cancel()
    },
    error(msg) {
      this.$notify({
        title: '错误',
        message: msg,
        type: 'error',
        duration: 2000
      })
    },
    onRemove(file) {
      this.currentArr = this.currentArr.filter(item => {
        return item.id !== file.id
      })
      // 如果是上传成功的才会触发，文件名重复等导致的不触发
      if (file.completed && this.isdel) {
        const attachmentno = []
        const delArr = this.tableData.filter(item => {
          return item.name == file.name
        })
        attachmentno.push(delArr[0].url)
        console.log('attachmentno', attachmentno)

        const params = {
          taskno: this.investigateNo,
          param: attachmentno
        }
        deleteAttachmentFileNew(params).then(response => {
          console.log(response)
          if (response.status === '1') {
            this.tableData = this.tableData.filter(item => {
              return item.name !== file.name
            })
            this.$message.success('删除成功！')
          } else {
            this.$message.error(response.data.errMsg)
          }
        })
      }
    },
    // 删除操作
    handleDelete(index, row) {
      // 二次确认删除
      this.$confirm('确定要删除吗？', '提示', {
        type: 'warning'
      }).then(async() => {
        const attachmentno = []
        attachmentno.push(row.url)
        const params = {
          taskno: this.investigateNo,
          param: attachmentno
        }
        deleteAttachmentFileNew(params)
        const response = await deleteAttachmentFileNew(params)
        console.log(response)
        if (response.status === '1') {
          this.tableData = this.tableData.filter(item => {
            return item.url !== row.url
          })
          this.$message.success('删除成功！')
        } else {
          this.$message.error(response.data.errMsg)
        }
      }
      )
    },
    uploadFiles() {
      this.uploadVisible = true
    },
    // 下载
    async handleDownload(index, file) {
      const name = file.name
      const attachmentNo = file.url
      const requestData = {
        fileName: encrypt(name),
        attachmentNo: encrypt(attachmentNo),
        downRequestType: encrypt('1'),
        taskNo: encrypt(this.investigateNo),
        source: encrypt(''),
        fileType: encrypt('')
      }
      const params = `fileName=${requestData.fileName}&attachmentNo=${requestData.attachmentNo}&downRequestType=${requestData.downRequestType}&taskNo=${requestData.taskNo}&source=${requestData.source}&fileType=${requestData.fileType}`
      const url = `${process.env.BASE_API}/invest/survey/getDownloadFile?${params}`
      window.open(url)
    },
    dialogClosed() {
      // this.$refs.uploader.uploader.cancel()
      this.uploadVisible = false
    }
  }
}
</script>

<style scoped>
  .uploader-ui {
    padding: 15px;
    margin: 0 auto ;
    font-size: 12px;
    font-family: Microsoft YaHei;
    box-shadow: 0 0 10px rgba(0, 0, 0, .4);
  }
  .uploader-ui .uploader-btn {
    margin-right: 4px;
    font-size: 12px;
    border-radius: 3px;
    color: #FFF;
    background-color: #409EFF;
    border-color: #409EFF;
    display: inline-block;
    line-height: 1;
    white-space: nowrap;
  }
  .uploader-ui .uploader-list {
    max-height: 440px;
    overflow: auto;
    overflow-x: hidden;
    overflow-y: auto;
  }
   .uploader-ui .uploader-list /deep/ .uploader-file[status="success"] .uploader-file-remove{
       display: block
   }
</style>

```

## XLSX（生成Excel）
https://www.npmjs.com/package/xlsx
### 代码（根据table或者json转换为Excel）
```js
/* eslint-disable */
require('script-loader!file-saver');
import XLSX from 'xlsx'

function generateArray(table) {
  var out = [];
  var rows = table.querySelectorAll('tr');
  var ranges = [];
  for (var R = 0; R < rows.length; ++R) {
    var outRow = [];
    var row = rows[R];
    var columns = row.querySelectorAll('td');
    for (var C = 0; C < columns.length; ++C) {
      var cell = columns[C];
      var colspan = cell.getAttribute('colspan');
      var rowspan = cell.getAttribute('rowspan');
      var cellValue = cell.innerText;
      if (cellValue !== "" && cellValue == +cellValue) cellValue = +cellValue;

      //Skip ranges
      ranges.forEach(function (range) {
        if (R >= range.s.r && R <= range.e.r && outRow.length >= range.s.c && outRow.length <= range.e.c) {
          for (var i = 0; i <= range.e.c - range.s.c; ++i) outRow.push(null);
        }
      });

      //Handle Row Span
      if (rowspan || colspan) {
        rowspan = rowspan || 1;
        colspan = colspan || 1;
        ranges.push({
          s: {
            r: R,
            c: outRow.length
          },
          e: {
            r: R + rowspan - 1,
            c: outRow.length + colspan - 1
          }
        });
      };

      //Handle Value
      outRow.push(cellValue !== "" ? cellValue : null);

      //Handle Colspan
      if (colspan)
        for (var k = 0; k < colspan - 1; ++k) outRow.push(null);
    }
    out.push(outRow);
  }
  return [out, ranges];
};

function datenum(v, date1904) {
  if (date1904) v += 1462;
  var epoch = Date.parse(v);
  return (epoch - new Date(Date.UTC(1899, 11, 30))) / (24 * 60 * 60 * 1000);
}

function sheet_from_array_of_arrays(data, opts) {
  var ws = {};
  var range = {
    s: {
      c: 10000000,
      r: 10000000
    },
    e: {
      c: 0,
      r: 0
    }
  };
  for (var R = 0; R != data.length; ++R) {
    for (var C = 0; C != data[R].length; ++C) {
      if (range.s.r > R) range.s.r = R;
      if (range.s.c > C) range.s.c = C;
      if (range.e.r < R) range.e.r = R;
      if (range.e.c < C) range.e.c = C;
      var cell = {
        v: data[R][C]
      };
      if (cell.v == null) continue;
      var cell_ref = XLSX.utils.encode_cell({
        c: C,
        r: R
      });

      if (typeof cell.v === 'number') cell.t = 'n';
      else if (typeof cell.v === 'boolean') cell.t = 'b';
      else if (cell.v instanceof Date) {
        cell.t = 'n';
        cell.z = XLSX.SSF._table[14];
        cell.v = datenum(cell.v);
      } else cell.t = 's';

      ws[cell_ref] = cell;
    }
  }
  if (range.s.c < 10000000) ws['!ref'] = XLSX.utils.encode_range(range);
  return ws;
}

function Workbook() {
  if (!(this instanceof Workbook)) return new Workbook();
  this.SheetNames = [];
  this.Sheets = {};
}

function s2ab(s) {
  var buf = new ArrayBuffer(s.length);
  var view = new Uint8Array(buf);
  for (var i = 0; i != s.length; ++i) view[i] = s.charCodeAt(i) & 0xFF;
  return buf;
}

export function export_table_to_excel(id) {
  var theTable = document.getElementById(id);
  var oo = generateArray(theTable);
  var ranges = oo[1];

  /* original data */
  var data = oo[0];
  var ws_name = "SheetJS";

  var wb = new Workbook(),
    ws = sheet_from_array_of_arrays(data);

  /* add ranges to worksheet */
  // ws['!cols'] = ['apple', 'banan'];
  ws['!merges'] = ranges;

  /* add worksheet to workbook */
  wb.SheetNames.push(ws_name);
  wb.Sheets[ws_name] = ws;

  var wbout = XLSX.write(wb, {
    bookType: 'xlsx',
    bookSST: false,
    type: 'binary'
  });

  saveAs(new Blob([s2ab(wbout)], {
    type: "application/octet-stream"
  }), "test.xlsx")
}

export function export_json_to_excel({
  header,
  data,
  filename,
  autoWidth = true,
  bookType=  'xlsx'
} = {}) {
  /* original data */
  filename = filename || 'excel-list'
  data = [...data]
  data.unshift(header);
  var ws_name = "SheetJS";
  var wb = new Workbook(),
    ws = sheet_from_array_of_arrays(data);

  if (autoWidth) {
    /*设置worksheet每列的最大宽度*/
    const colWidth = data.map(row => row.map(val => {
      /*先判断是否为null/undefined*/
      if (val == null) {
        return {
          'wch': 10
        };
      }
      /*再判断是否为中文*/
      else if (val.toString().charCodeAt(0) > 255) {
        return {
          'wch': val.toString().length * 2
        };
      } else {
        return {
          'wch': val.toString().length
        };
      }
    }))
    /*以第一行为初始值*/
    let result = colWidth[0];
    for (let i = 1; i < colWidth.length; i++) {
      for (let j = 0; j < colWidth[i].length; j++) {
        if (result[j]['wch'] < colWidth[i][j]['wch']) {
          result[j]['wch'] = colWidth[i][j]['wch'];
        }
      }
    }
    ws['!cols'] = result;
  }

  /* add worksheet to workbook */
  wb.SheetNames.push(ws_name);
  wb.Sheets[ws_name] = ws;

  var wbout = XLSX.write(wb, {
    bookType: bookType,
    bookSST: false,
    type: 'binary'
  });
  saveAs(new Blob([s2ab(wbout)], {
    type: "application/octet-stream"
  }), `${filename}.${bookType}`);
}

```



# 数字计算
- number-precision  https://github.com/nefe/number-precision
- mathjs   https://github.com/josdejong/mathjs/
## 处理大整数
- bigInt类型在es10中被提出，现在Chrome中已经可以使用，使用bigInt可以操作超过最大安全数字的数字。
- node-bignum https://github.com/justmoon/node-bignum
- node-bigint https://github.com/substack/node-bigint

